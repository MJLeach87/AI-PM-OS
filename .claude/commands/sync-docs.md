Synchronize PM OS documentation.

Invoke the Documentation Maintainer to:
1. Audit cross-references across `.claude/CLAUDE.md`, `README.md`, `QUICK_START.md`, and `pm-os-reference/identity/ROADMAP.md`
2. Check that phase completion status is current
3. Identify stale references or outdated paths
4. Propose or apply updates (confirm with user before writing)

Pass context via $ARGUMENTS if relevant (e.g., "after Phase 5 completion", "agent update: engineering-partner")
